/* import { useState } from "#app";
import { getValue } from "./Functions";

export const useAuth = () => {
  getValue("loginUsername");
  const userName = useState("userName", () => null);

  const isUserLoggedIn = () => userName.value !== "" && userName.value !== null;

  const setUser = (name) => {
    userName.value = name;
  };

  return { userName, isUserLoggedIn, setUser };
};
 */
